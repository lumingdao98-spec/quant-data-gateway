# V3.11 / InfoFix V12 信息面语义关系与详情页增强

## 修复重点

1. **修复“增持其他股票”误判为当前股票增持**
   - 新增目标标的关系识别：`_target_relation_analysis()`。
   - 示例：`大幅度增持小米集团，贵州茅台退出持仓前十`。
     - 对 `600519 贵州茅台`：识别为“机构持仓下降/退出前十”，不是增持。
     - 对 `小米集团`：识别为“机构持仓上升”。
   - 区分：公司回购/股东增持、机构基金加仓、机构基金减仓、退出重仓前十。

2. **信息面独立详情页**
   - 新增页面：`/info?symbol=600519&name=贵州茅台`
   - 支持分页、时间正序/倒序、分类过滤、未知时间过滤、查看去重组、来源/分类占比。
   - 筛选页右侧信息面区域新增“打开信息面详情页”按钮。

3. **信息面分页接口**
   - `/api/info/items/{symbol}?page=1&page_size=30&sort=desc&category=&include_unknown_date=true`
   - 旧接口仍兼容：不传 `page` 时保持返回旧列表格式。

4. **自定义抓取数量**
   - `/api/info/analyze/{symbol}?limit=30~500&force=true`
   - 独立信息面详情页可自定义抓取数量。

5. **后台命令窗进度输出**
   - 信息面分析时输出：开始分析、历史库读取、多源抓取、各源返回数量、去重数量、保存数量、最终得分。
   - 前端详情页同时提示命令窗可查看进度。

6. **日期识别增强**
   - 支持：`2025年07月21日 07:07:34`、`2025-7-21 7:07`、`7月21日 07:07`、相对时间、URL 中的 `/2025/07/210707...`。
   - 对无法从标题/上下文识别的新闻，仍归为“时间未知”，并可在详情页单独过滤。

7. **信息去重和统计增强**
   - 分页接口返回分类占比、来源占比、时间已知/未知数量、高重合去重组。
   - 事件去重仍以 `event_key` 为主，避免转载和同一事件重复计分。

## 说明

本版仍不把社区传闻、网页搜索碎片、无法核验信息当成公司事实。信息面结论仅作为筛选辅助，关键公告/财报需人工核验原文。
