# InfoFix V16.2 源头候选准入与详情正文准入修复

本版本针对“抓取 99 条，后置清洗丢弃 41 条，说明源头抓取逻辑太松”的问题做增量修复。

## 核心变化

1. 新浪/同花顺股票专页启用 `strict_article + deep_validate`。
2. 列表页只提取疑似真实文章/公告详情 URL，拒绝 F10 栏目页、菜单页、行情页、表格页。
3. 候选链接进入详情页读取正文，正文必须具备文章句子结构、财经/公司关系、有效中文内容，才进入候选池。
4. 列表页上下文不再作为新闻摘要入库，仅作为发布日期兜底。
5. 新增 `_is_probable_article_url`、`_is_article_detail_text`、`_extract_article_body_text`。
6. 缓存 key 升级为 `cn_v315_v16_2`，避免继续命中旧脏缓存。

## 预期效果

新浪/同花顺源返回数量会明显下降，但有效率上升；清洗丢弃数量应主要来自历史库旧脏数据或少量页面结构变动，而不是新抓取源头大量污染。
