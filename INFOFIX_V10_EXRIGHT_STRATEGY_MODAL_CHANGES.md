# V3.9 / InfoFix V10：复权回撤与分类策略选择器

## 本版改动

1. **高位回撤默认考虑除权影响**
   - 筛选页新增“复权口径”选项：前复权 qfq / 不复权 none / 后复权 hfq。
   - 默认使用“前复权”日K参与 250 日位置、高位回撤、低点反弹和多数技术指标计算。
   - 目的：降低分红、送转、除权导致的价格断层，使“高位回撤充分”不被假缺口误判。

2. **不同复权口径单独缓存**
   - 不复权继续缓存到 `1d`。
   - 前复权缓存到 `1d:qfq`。
   - 后复权缓存到 `1d:hfq`。
   - 避免前复权和不复权K线混用。

3. **策略选择器改为独立弹窗**
   - 左侧只保留已选策略摘要和“打开分类策略选择器”按钮。
   - 策略在弹窗中按类别展示：低位/价值、K线趋势、趋势跟随、动量/反转、量价/盘口、技术形态、支撑阻力、时间周期、能量/情绪、新闻/基本面、ETF/基金、均值回归、风控过滤。
   - 支持分类切换、默认组合、全选、清空和完成。

4. **新增复权回撤校正策略项**
   - 默认启用“复权回撤校正”。
   - 若使用前复权/后复权，作为策略标签提示；若用户切到不复权且出现大幅回撤，则提示“未复权回撤可能受除权影响”。

5. **标签解释同步增强**
   - 低位/回撤类标签解释中新增“K线复权口径”。
   - 明确说明默认用前复权K线降低除权、分红、送转对高位回撤判断的污染。

## 注意

- 复权口径主要影响历史价格位置、回撤、均线、BOLL、ATR、支撑压力等历史K线指标。
- 实时行情仍来自当前未复权快照；前复权通常以当前价为锚，因此短线筛选更推荐 qfq。
- 不复权适合查看原始交易价格，但不适合直接判断长期回撤。
